import 'package:flutter/material.dart';

import 'package:quac/screens/login/login_back.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold
    (body: Background(),
    resizeToAvoidBottomPadding: false,);
  }
}
